---
title: "Next.js ORM (Prisma)"
label: "Next.js ORM (Prisma)"
---

Next.jsはPrismaを使用して、データベース操作を簡素化します。

## モデルの定義

```prisma
model Book {
  id     Int    @id @default(autoincrement())
  title  String
  author String
}
```

- **`@id`**: 主キーを定義
- **`@default(autoincrement())`**: 自動インクリメントを設定
